{
  "name": "@rcc/event-bus",
  "version": "0.1.0",
  "private": true,
  "description": "Typed pub/sub for RCC domain events (PRD section 4 and 23). In-memory implementation for local dev; swap the transport for Redis/SQS/Kafka in production without changing call sites.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@rcc/shared-types": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
